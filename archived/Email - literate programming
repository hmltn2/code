# This program follows a specific design aesthetic:
# 1. Every possible word or symbol is wrapped, except for keywords like "if".
# 2. Variables and method names are kept short and simple.
# 3. Methods have no more than 3 lines of code.
# 4. Classes are wrapped, and their methods are also wrapped to avoid the use of periods in method bodies.

import smtplib

def main():
    send_email()

def send_email():
    server = smtplib.SMTP('smtp.gmail.com', 587)
    server.starttls()
    server.login('example@gmail.com', 'password')
    server.sendmail('example@gmail.com', 'recipient@example.com', 'Subject: Test\n\nThis is a test email')
    server.quit()

if __name__ == "__main__":
    main()

import smtplib

def send_email():
    start_tls()
    login()
    compose_message()
    send()

def start_tls():
    smtp.starttls()

def login():
    smtp.login(username, password)

def compose_message():
    message = "Subject: {}\n\n{}".format(subject, body)

def send():
    smtp.sendmail(sender, recipient, message)

if __name__ == '__main__':
    smtp = smtplib.SMTP(smtp_server, smtp_port)
    send_email()
    smtp.quit()





